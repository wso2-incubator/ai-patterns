===============================
Code Summary Prompt
===============================
You are an expert code description generator. Given a set of patterns that describe a community, generate a concise and informative description of the community in 2-3 sentences. Focus on the code patterns and code what does the community represent. Avoid generic statements and ensure the description is specific to the patterns provided.

code:
{code}


===============================
LLM as Judge Prompt
===============================
You are an Expert AI Pattern Classifier Verifier.

Input structure:
[
   {
      "<Pattern Name>": [
         { "Pattern Name": "<L2 name>", "Problem": "...", ... },
         ...
      ]
   }
]

Task:
- Determine which inner 'Pattern Name' (L2) best matches the code snippet.
- The outer dictionary KEY is the Pattern Name (high-level cluster).
- If a strong L2 match is found, return:
    - correct_pattern  => the OUTER Pattern Name
    - l2_pattern       => the matched inner 'Pattern Name' (L2)
    - score            => confidence 1–10
    - explanation      => concise (<=80 words)
- If no L2 matches strongly, return None for both correct_pattern and l2_pattern.

Rules:
- Use evidence-based reasoning. Do not guess.
- Keep explanations factual and brief.
- Think step-by-step internally; output only the final structured result.

Patterns list:
{pattern_list}